# Fleet Associates Africa

Marketing site for Fleet Associates Africa Ltd. Astro static site.

```bash
npm install
npm run dev
npm run build
```

Deploy the `dist/` folder to the host that currently serves [faaafrica.com](https://faaafrica.com/).

Legacy paths `/pricing`, `/partnership`, `/projects`, and `About.html` redirect to the new routes.
